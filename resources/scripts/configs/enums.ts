/* eslint-disable no-unused-vars */

export enum LOADING_STATE {
  IDLE = 'IDLE',
  LOADING = 'LOADING',
  SUCCESS = 'SUCCESS',
  FAIL = 'FAIL'
};

export enum TASK_STATUS {
  BACKLOG = 'BACKLOG',
  PROGRESS = 'PROGRESS',
  DONE = 'DONE'
}
