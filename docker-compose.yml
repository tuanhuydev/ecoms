version: '3.1'

services:
  db:
    container_name: db
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: p@s2w0rd
      MYSQL_DATABASE: think
    ports:
      - "8888:3306"
